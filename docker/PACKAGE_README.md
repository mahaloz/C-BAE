# Function-Name Evaluation Runtime

This package provides isolated reverser and grader stages for a blind static
function-name recovery benchmark. Dataset identities, binaries, symbol sources,
and ground truth are deliberately not included in the runtime image.
